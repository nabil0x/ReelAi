--prefer-binary
# NOTE: Kaggle already ships torch, transformers, accelerate, safetensors,
# opencv-python, librosa, soundfile, numpy, Pillow, matplotlib. Do NOT list
# them here — listing them sends pip into dependency-resolver backtracking
# (the "Downloading accelerate-1.x.y ... 1.x.y-1 ..." crawl). Import them
# straight from the image instead.
paddlepaddle-gpu
paddleocr
# coqui-tts is installed at runtime with --no-deps (see scripts/04_tts_shootout.py);
# only its light inference deps are listed here:
anyascii
coqpit-config
einops
inflect
num2words
pysbd
monotonic-alignment-search
ko-speech-tools
loguru
PyYAML>=6.0.3,<6.1
