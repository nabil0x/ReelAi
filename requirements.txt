--prefer-binary
# Kaggle ships torch, transformers, accelerate, safetensors, opencv-python,
# librosa, soundfile, numpy, Pillow, matplotlib, huggingface-hub.  Do NOT
# list them here — listing them sends pip into dependency-resolver backtracking.
# Import them straight from the Kaggle image instead.
#
# PP-OCRv5 (paddlepaddle-gpu, paddleocr) is OPTIONAL — install separately
# via `bash scripts/install_ocr.sh` only if you need it.
#
# coqui-tts is installed at runtime with --no-deps (see 04_tts_shootout.py);
# only its light inference deps are listed here:
anyascii
coqpit-config
einops
inflect
num2words
pysbd
monotonic-alignment-search
ko-speech-tools
loguru
PyYAML>=6.0.3,<6.1
